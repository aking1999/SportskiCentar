@model SportskiCentar.Models.Customer
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutForCart.cshtml";
}

<h2>Add a new customer</h2>

@using (Html.BeginForm("AddNewCustomer", "Customers", FormMethod.Post))
{
    <div class="form-group">
        @Html.LabelFor(c => c.FirstName)
        @Html.TextBoxFor(c => c.FirstName, new { @class = "form-control" })
        @Html.ValidationMessageFor(c => c.FirstName)
    </div>

    <div class="form-group">
        @Html.LabelFor(c => c.LastName)
        @Html.TextBoxFor(c => c.LastName, new { @class = "form-control" })
        @Html.ValidationMessageFor(c => c.LastName)
    </div>

    <div class="form-group">
        @Html.LabelFor(c => c.JMBG)
        @Html.TextBoxFor(c => c.JMBG, new { @class = "form-control" })
        @Html.ValidationMessageFor(c => c.JMBG)
    </div>

    <div class="form-group">
        @Html.LabelFor(c => c.Address)
        @Html.TextBoxFor(c => c.Address, new { @class = "form-control" })
        @Html.ValidationMessageFor(c => c.Address)
    </div>

    <button type="submit" class="btn btn-success">Save</button>
}

<script>
     function NewCustomerNotification() {
         //document.addEventListener('DOMContentLoaded', function () { toastr.error("User with the same JMBG exists. Please try again."); });
         document.addEventListener('DOMContentLoaded', function () { toastr.error("Customer with the same JMBG exists. Please try again."); });
    }
</script>



@*Za novog korisnika*@
@if (ViewBag.NewCustomerNotification != null)
{
    <script>
        @Html.Raw(ViewBag.NewCustomerNotification);
    </script>
}
